using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KillAnt : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
   public void Kill(GameObject obj)
    {
        Destroy(obj);
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
